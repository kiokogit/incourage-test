config:
  target: "http://localhost:8000"
  phases:
    - duration: 60
      arrivalRate: 50
scenarios:
  - flow:
      - post:
          url: "/login"
          json:
            username: "q"
            password: "q"

